kafka:
  bootstrap_servers: "redpanda:29092"
  topic: "streamflow.events"

spark:
  app_name: "PrototypeSmokeTest"
  raw_output_path: "/opt/streamflow/data/raw"

store_rules:
  critical_stock_threshold: 5